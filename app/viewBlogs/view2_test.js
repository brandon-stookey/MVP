'use strict';

describe('myApp.viewBlogs module', function() {

  beforeEach(module('myApp.viewBlogs'));

  describe('viewBlogs controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));
//hello!
  });
});